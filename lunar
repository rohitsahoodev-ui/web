<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>LunarHost | Elite Hosting Solutions</title>
    <link rel="icon" href="https://cdn.discordapp.com/icons/1459845419994448130/a_009ae7fb5523d45c4f5a3ab86f5a4e2a.gif?size=2048">
    
    <!-- Premium Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lexend:wght@300;400;600;700;800&family=Inter:wght@300;400;600&display=swap" rel="stylesheet">
    
    <!-- Icons -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css">

    <style>
        :root {
            --primary: #8a2be2;
            --primary-glow: rgba(138, 43, 226, 0.4);
            --accent: #00d2ff;
            --accent-glow: rgba(0, 210, 255, 0.3);
            --bg-dark: #050505;
            --card-bg: rgba(255, 255, 255, 0.03);
            --border: rgba(255, 255, 255, 0.08);
            --text-main: #f8f9fa;
            --text-dim: #9ca3af;
            --transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', sans-serif;
            background-color: var(--bg-dark);
            color: var(--text-main);
            overflow-x: hidden;
            line-height: 1.6;
        }

        h1, h2, h3, .logo {
            font-family: 'Lexend', sans-serif;
        }

        /* Animated Mesh Background */
        .mesh-bg {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -3;
            background: 
                radial-gradient(circle at 20% 30%, rgba(106, 13, 173, 0.15) 0%, transparent 40%),
                radial-gradient(circle at 80% 70%, rgba(0, 210, 255, 0.1) 0%, transparent 40%);
        }

        .bg-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            background: linear-gradient(to bottom, rgba(5, 5, 5, 0.8), rgba(5, 5, 5, 0.95)), 
                        url('https://motionbgs.com/media/745/by-the-fire-minecraft.jpg');
            background-size: cover;
            background-position: center;
            opacity: 0.6;
        }

        #snowflake-canvas {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
            opacity: 0.5;
        }

        /* Navbar Upgrade */
        nav {
            position: fixed;
            top: 20px;
            left: 50%;
            transform: translateX(-50%);
            width: 90%;
            max-width: 1200px;
            padding: 0.8rem 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            background: rgba(15, 15, 15, 0.7);
            backdrop-filter: blur(20px);
            border: 1px solid var(--border);
            border-radius: 100px;
            z-index: 1000;
            transition: var(--transition);
        }

        nav.scrolled {
            width: 100%;
            top: 0;
            border-radius: 0;
            border-top: none;
            border-left: none;
            border-right: none;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
            font-weight: 800;
            font-size: 1.3rem;
            color: #fff;
            text-decoration: none;
            letter-spacing: -0.5px;
        }

        .logo img {
            width: 38px;
            height: 38px;
            border-radius: 50%;
            border: 2px solid var(--primary);
        }

        .nav-links {
            display: flex;
            gap: 1.5rem;
            list-style: none;
        }

        .nav-links a {
            text-decoration: none;
            color: var(--text-dim);
            font-weight: 500;
            font-size: 0.9rem;
            transition: var(--transition);
            cursor: pointer;
            padding: 8px 12px;
            border-radius: 20px;
        }

        .nav-links a:hover, .nav-links a.active {
            color: #fff;
            background: rgba(255, 255, 255, 0.05);
        }

        .discord-btn {
            background: #5865F2;
            padding: 0.6rem 1.4rem;
            border-radius: 50px;
            color: white;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 8px;
            transition: var(--transition);
        }

        .discord-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(88, 101, 242, 0.3);
        }

        /* View Management */
        .view-container {
            min-height: 100vh;
            padding: 140px 5% 60px;
            display: none;
            opacity: 0;
            transform: scale(0.98);
            transition: var(--transition);
        }

        .view-container.active {
            display: block;
            opacity: 1;
            transform: scale(1);
        }

        /* Hero Upgrade */
        .hero {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            max-width: 1000px;
            margin: 0 auto;
            padding: 40px 0;
        }

        .hero h1 {
            font-size: clamp(2.8rem, 10vw, 5.5rem);
            font-weight: 900;
            margin-bottom: 1.5rem;
            background: linear-gradient(135deg, #fff 30%, var(--accent) 100%);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            line-height: 1;
            letter-spacing: -2px;
        }

        .hero p {
            font-size: 1.25rem;
            color: var(--text-dim);
            max-width: 750px;
            margin-bottom: 3rem;
            font-weight: 300;
        }

        /* Sections & Badges */
        .section-header {
            text-align: center;
            margin-bottom: 4rem;
        }

        .section-header h2 {
            font-size: 3rem;
            font-weight: 800;
            letter-spacing: -1px;
        }

        .badge {
            background: linear-gradient(90deg, var(--primary-glow), transparent);
            border-left: 3px solid var(--primary);
            color: #fff;
            padding: 6px 16px;
            font-size: 0.75rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 2px;
            margin-bottom: 1.5rem;
            display: inline-block;
        }

        /* Premium Tabs */
        .category-tabs {
            background: rgba(255, 255, 255, 0.03);
            padding: 6px;
            border-radius: 50px;
            display: inline-flex;
            gap: 5px;
            margin-bottom: 4rem;
            border: 1px solid var(--border);
        }

        .tab-btn {
            background: transparent;
            border: none;
            color: var(--text-dim);
            padding: 10px 24px;
            border-radius: 50px;
            cursor: pointer;
            transition: var(--transition);
            font-weight: 600;
            font-size: 0.9rem;
        }

        .tab-btn.active {
            background: #fff;
            color: #000;
        }

        /* Pricing Grid & Cards Upgrade */
        .plans-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
            gap: 2.5rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .plan-card {
            background: linear-gradient(145deg, rgba(255, 255, 255, 0.05) 0%, transparent 100%);
            border: 1px solid var(--border);
            border-radius: 24px;
            padding: 3rem 2rem;
            position: relative;
            transition: var(--transition);
            display: flex;
            flex-direction: column;
            overflow: hidden;
        }

        .plan-card::after {
            content: '';
            position: absolute;
            top: 0; left: 0; right: 0; bottom: 0;
            border-radius: 24px;
            padding: 1px;
            background: linear-gradient(145deg, var(--border), transparent, var(--border));
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            -webkit-mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: exclude;
            -webkit-mask-composite: destination-out;
            pointer-events: none;
        }

        .plan-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.07);
            border-color: var(--primary);
            box-shadow: 0 30px 60px -12px rgba(0, 0, 0, 0.5), 0 0 20px var(--primary-glow);
        }

        .plan-name { font-size: 1.1rem; color: var(--text-dim); text-transform: uppercase; letter-spacing: 2px; font-weight: 700; margin-bottom: 1rem; }
        .plan-price { font-size: 2.5rem; font-weight: 800; color: #fff; margin-bottom: 2rem; font-family: 'Lexend'; }
        .plan-price span { font-size: 1rem; color: var(--text-dim); font-weight: 400; }
        
        .plan-specs { list-style: none; margin-bottom: 2.5rem; flex-grow: 1; border-top: 1px solid var(--border); pt: 2rem; padding-top: 2rem; }
        .plan-specs li { margin-bottom: 1rem; color: var(--text-dim); font-size: 0.95rem; display: flex; align-items: center; gap: 12px; }
        .plan-specs li i { color: var(--accent); font-size: 1rem; }
        
        .plan-btn {
            width: 100%;
            padding: 1.1rem;
            border-radius: 14px;
            border: none;
            background: #fff;
            color: #000;
            font-weight: 700;
            font-size: 1rem;
            cursor: pointer;
            transition: var(--transition);
            text-align: center;
            text-decoration: none;
        }

        .plan-btn:hover {
            transform: scale(1.02);
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.3);
        }

        .popular-tag {
            position: absolute;
            top: 20px;
            right: -30px;
            background: var(--primary);
            color: white;
            padding: 5px 40px;
            font-size: 0.7rem;
            font-weight: 800;
            transform: rotate(45deg);
        }

        /* Features Upgrade */
        .feature-box {
            background: var(--card-bg);
            border: 1px solid var(--border);
            padding: 3rem 2rem;
            border-radius: 28px;
            text-align: left;
            transition: var(--transition);
        }

        .feature-box:hover {
            border-color: var(--accent);
            background: rgba(0, 210, 255, 0.02);
        }

        .feature-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(135deg, var(--primary), var(--accent));
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            color: white;
            margin-bottom: 2rem;
        }

        /* Footer Upgrade */
        footer {
            margin-top: 150px;
            padding: 100px 5% 50px;
            background: radial-gradient(circle at top, rgba(138, 43, 226, 0.05), transparent);
            border-top: 1px solid var(--border);
        }

        .footer-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 4rem;
            max-width: 1200px;
            margin: 0 auto;
            text-align: left;
        }

        .footer-brand h3 { font-size: 1.5rem; margin-bottom: 1rem; }
        .footer-links h4 { margin-bottom: 1.5rem; color: #fff; font-size: 1rem; }
        .footer-links ul { list-style: none; }
        .footer-links li { margin-bottom: 0.8rem; }
        .footer-links a { color: var(--text-dim); text-decoration: none; transition: var(--transition); font-size: 0.9rem; }
        .footer-links a:hover { color: var(--accent); padding-left: 5px; }

        @media (max-width: 768px) {
            nav { width: 95%; padding: 0.6rem 1rem; border-radius: 30px; }
            .nav-links { display: none; }
            .hero h1 { font-size: 3.2rem; }
            .plans-grid { grid-template-columns: 1fr; }
        }
    </style>
</head>
<body>

    <div class="mesh-bg"></div>
    <div class="bg-overlay"></div>
    <canvas id="snowflake-canvas"></canvas>

    <!-- Navigation -->
    <nav id="navbar">
        <a class="logo" onclick="navigateTo('home')">
            <img src="https://cdn.discordapp.com/icons/1459845419994448130/a_009ae7fb5523d45c4f5a3ab86f5a4e2a.gif?size=2048" alt="Logo">
            LunarHost
        </a>
        <ul class="nav-links">
            <li><a onclick="navigateTo('home')" id="nav-home" class="active">Home</a></li>
            <li><a onclick="navigateTo('plans')" id="nav-plans">Servers</a></li>
            <li><a onclick="navigateTo('bots')" id="nav-bots">Bots</a></li>
            <li><a onclick="navigateTo('features')" id="nav-features">Why Us</a></li>
        </ul>
        <a href="https://discord.gg/UdsyzKN7Up" class="discord-btn" target="_blank">
            <i class="fab fa-discord"></i> Discord
        </a>
    </nav>

    <!-- HOME VIEW -->
    <div id="home" class="view-container active">
        <section class="hero">
            <span class="badge">Next-Gen Hosting</span>
            <h1>Performance that<br>shatters limits.</h1>
            <p>Elevate your digital presence with enterprise-grade Ryzen™ and EPYC™ hardware. Low latency, high uptime, and premium support.</p>
            <div style="display: flex; gap: 1.5rem; flex-wrap: wrap; justify-content: center;">
                <button class="plan-btn" style="padding: 1.2rem 2.5rem; width: auto;" onclick="navigateTo('plans')">View All Servers</button>
                <a href="https://discord.gg/UdsyzKN7Up" class="plan-btn" style="padding: 1.2rem 2.5rem; width: auto; background: rgba(255,255,255,0.05); color: #fff; border: 1px solid var(--border);">Consult an Expert</a>
            </div>
        </section>
        
        <div style="margin-top: 100px; text-align: center;">
            <p style="color: var(--text-dim); font-size: 0.8rem; text-transform: uppercase; letter-spacing: 3px; margin-bottom: 3rem;">Trusted Infrastructure Partners</p>
            <div style="display: flex; justify-content: center; gap: 4rem; opacity: 0.4; flex-wrap: wrap;">
                <span style="font-size: 1.5rem; font-weight: 800;">AMD RYZEN</span>
                <span style="font-size: 1.5rem; font-weight: 800;">INTEL XEON</span>
                <span style="font-size: 1.5rem; font-weight: 800;">NVME GEN4</span>
                <span style="font-size: 1.5rem; font-weight: 800;">ASHEOCLOUD</span>
            </div>
        </div>
    </div>

    <!-- PLANS VIEW -->
    <div id="plans" class="view-container">
        <div class="section-header">
            <span class="badge">Performance Tiers</span>
            <h2>Premium Server Infrastructure</h2>
            <p>Select the power you need to scale your project.</p>
        </div>

        <div style="text-align: center;">
            <div class="category-tabs">
                <button class="tab-btn active" onclick="switchPlanCategory('light', this)">Light</button>
                <button class="tab-btn" onclick="switchPlanCategory('budget', this)">Budget</button>
                <button class="tab-btn" onclick="switchPlanCategory('performance', this)">Performance</button>
            </div>
        </div>

        <div id="light-plans" class="plan-group">
            <div class="plans-grid" id="light-grid"></div>
        </div>
        <div id="budget-plans" class="plan-group" style="display:none">
            <div class="plans-grid" id="budget-grid"></div>
        </div>
        <div id="performance-plans" class="plan-group" style="display:none">
            <div class="plans-grid" id="performance-grid"></div>
        </div>
    </div>

    <!-- BOTS VIEW -->
    <div id="bots" class="view-container">
        <div class="section-header">
            <span class="badge">Bot Ecosystem</span>
            <h2>Discord Bot & Sound Hosting</h2>
            <p>High-uptime containers for 24/7 online presence.</p>
        </div>
        <div class="plans-grid" id="bot-grid"></div>
        
        <div class="section-header" style="margin-top: 100px;">
            <h2>Sound & LavaLink Hosting</h2>
        </div>
        <div class="plans-grid" id="voice-grid"></div>
    </div>

    <!-- FEATURES VIEW -->
    <div id="features" class="view-container">
        <div class="section-header">
            <span class="badge">Our DNA</span>
            <h2>Why LunarHost Leads</h2>
        </div>
        <div class="plans-grid" style="grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));">
            <div class="feature-box">
                <div class="feature-icon"><i class="fas fa-rocket"></i></div>
                <h3>Gen4 NVMe Only</h3>
                <p>We don't use SATA. All servers are equipped with Samsung/WD Black Gen4 NVMe for instant data access.</p>
            </div>
            <div class="feature-box">
                <div class="feature-icon"><i class="fas fa-microchip"></i></div>
                <h3>DDR5 5400MHz RAM</h3>
                <p>Experience the bandwidth of next-gen memory that eliminates processing bottlenecks.</p>
            </div>
            <div class="feature-box">
                <div class="feature-icon"><i class="fas fa-shield-halved"></i></div>
                <h3>L7 DDoS Mitigation</h3>
                <p>Custom filters for Minecraft, FiveM, and Discord Bots to ensure zero packet loss during attacks.</p>
            </div>
            <div class="feature-box">
                <div class="feature-icon"><i class="fas fa-clock"></i></div>
                <h3>Instant Provisioning</h3>
                <p>No waiting in queues. Our automated system deploys your server in under 60 seconds.</p>
            </div>
        </div>
    </div>

    <footer>
        <div class="footer-grid">
            <div class="footer-brand">
                <a class="logo">LunarHost</a>
                <p style="color: var(--text-dim); margin-top: 1rem; font-size: 0.9rem;">Redefining digital infrastructure with high-performance hardware and elite security.</p>
            </div>
            <div class="footer-links">
                <h4>Products</h4>
                <ul>
                    <li><a onclick="navigateTo('plans')">Game Hosting</a></li>
                    <li><a onclick="navigateTo('bots')">Bot Hosting</a></li>
                    <li><a onclick="navigateTo('plans')">Dedicated Servers</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>Company</h4>
                <ul>
                    <li><a onclick="navigateTo('features')">About Us</a></li>
                    <li><a href="https://discord.gg/UdsyzKN7Up">Status Page</a></li>
                    <li><a href="https://discord.gg/UdsyzKN7Up">Legal</a></li>
                </ul>
            </div>
            <div class="footer-links">
                <h4>Social</h4>
                <ul>
                    <li><a href="https://discord.gg/UdsyzKN7Up">Discord</a></li>
                    <li><a href="#">Twitter</a></li>
                    <li><a href="#">Github</a></li>
                </ul>
            </div>
        </div>
        <div style="text-align: center; margin-top: 80px; padding-top: 40px; border-top: 1px solid var(--border);">
            <p style="color: var(--text-dim); font-size: 0.8rem;">&copy; 2026 LunarHost. Powered by Asheocloud.</p>
        </div>
    </footer>

    <script>
        const discordLink = "https://discord.gg/UdsyzKN7Up";

        const planTiers = ["Dirt", "Stone", "Iron", "Redstone", "Gold", "Diamond", "Netherite"];
        const lightPrices = [20, 40, 80, 160, 320, 480, 640];
        const lightSpecs = [
            { cpu: "100%", ram: "2GB", storage: "5GB SSD" },
            { cpu: "200%", ram: "4GB", storage: "10GB SSD" },
            { cpu: "300%", ram: "8GB", storage: "20GB SSD" },
            { cpu: "400%", ram: "16GB", storage: "40GB SSD" },
            { cpu: "500%", ram: "32GB", storage: "50GB SSD" },
            { cpu: "600%", ram: "48GB", storage: "60GB SSD" },
            { cpu: "800%", ram: "64GB", storage: "80GB SSD" }
        ];

        const budgetPrices = [40, 80, 160, 320, 640, 960, 1280];
        const performancePrices = [60, 120, 240, 480, 960, 1440, 1920];

        const botPlans = [
            { name: "Starter Tier", price: 29, cpu: "50%", ram: "512MB", storage: "500MB", backup: "1x" },
            { name: "Pro Tier", price: 59, cpu: "100%", ram: "1GB", storage: "1GB", backup: "1x" },
            { name: "Giant Tier", price: 89, cpu: "150%", ram: "3GB", storage: "3GB", backup: "1x" },
            { name: "Ultra Tier", price: 119, cpu: "200%", ram: "5GB", storage: "5GB", backup: "2x" }
        ];

        // Navbar Scroll Logic
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('navbar');
            if (window.scrollY > 50) nav.classList.add('scrolled');
            else nav.classList.remove('scrolled');
        });

        // Navigation
        function navigateTo(viewId) {
            document.querySelectorAll('.view-container').forEach(v => v.classList.remove('active'));
            document.querySelectorAll('.nav-links a').forEach(a => a.classList.remove('active'));
            
            const target = document.getElementById(viewId);
            target.classList.add('active');
            const navLink = document.getElementById('nav-' + viewId);
            if(navLink) navLink.classList.add('active');
            
            window.scrollTo(0, 0);
        }

        // Card Generators
        function generatePlanCards(containerId, cpuType, prices, specs) {
            const container = document.getElementById(containerId);
            planTiers.forEach((tier, i) => {
                const card = document.createElement('div');
                card.className = 'plan-card';
                if(tier === "Iron") card.innerHTML += '<div class="popular-tag">BEST VALUE</div>';
                card.innerHTML += `
                    <div class="plan-name">${tier}</div>
                    <div class="plan-price">₹${prices[i]}<span>/mo</span></div>
                    <ul class="plan-specs">
                        <li><i class="fas fa-check-circle"></i> ${cpuType} ${specs[i].cpu}</li>
                        <li><i class="fas fa-check-circle"></i> ${specs[i].ram} DDR5 RAM</li>
                        <li><i class="fas fa-check-circle"></i> ${specs[i].storage} NVMe</li>
                        <li><i class="fas fa-check-circle"></i> L7 DDoS Guard</li>
                    </ul>
                    <a href="${discordLink}" target="_blank" class="plan-btn">Deploy Now</a>
                `;
                container.appendChild(card);
            });
        }

        function generateBotCards(containerId, plans, type) {
            const container = document.getElementById(containerId);
            plans.forEach(p => {
                const card = document.createElement('div');
                card.className = 'plan-card';
                card.innerHTML = `
                    <div class="plan-name">${p.name}</div>
                    <div class="plan-price">₹${p.price}<span>/mo</span></div>
                    <ul class="plan-specs">
                        <li><i class="fas fa-microchip"></i> ${p.cpu} Isolated CPU</li>
                        <li><i class="fas fa-memory"></i> ${p.ram} RAM</li>
                        <li><i class="fas fa-hdd"></i> ${p.storage} Fast SSD</li>
                        <li><i class="fas fa-copy"></i> ${p.backup} Offsite Backup</li>
                    </ul>
                    <a href="${discordLink}" target="_blank" class="plan-btn">Launch ${type}</a>
                `;
                container.appendChild(card);
            });
        }

        function switchPlanCategory(category, btn) {
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active'));
            btn.classList.add('active');
            document.querySelectorAll('.plan-group').forEach(g => g.style.display = 'none');
            document.getElementById(category + '-plans').style.display = 'block';
        }

        // Canvas Particles
        const canvas = document.getElementById('snowflake-canvas');
        const ctx = canvas.getContext('2d');
        let particles = [];
        function resize() { canvas.width = window.innerWidth; canvas.height = window.innerHeight; }
        window.addEventListener('resize', resize);
        resize();

        class Particle {
            constructor() { this.reset(); this.y = Math.random() * canvas.height; }
            reset() {
                this.x = Math.random() * canvas.width;
                this.y = -10;
                this.radius = Math.random() * 2 + 0.5;
                this.speed = Math.random() * 0.8 + 0.2;
                this.wind = Math.random() * 0.2 - 0.1;
                this.opacity = Math.random() * 0.5;
            }
            update() {
                this.y += this.speed;
                this.x += this.wind;
                if (this.y > canvas.height) this.reset();
            }
            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.radius, 0, Math.PI * 2);
                ctx.fillStyle = `rgba(255, 255, 255, ${this.opacity})`;
                ctx.fill();
            }
        }

        function init() {
            for (let i = 0; i < 100; i++) particles.push(new Particle());
            generatePlanCards('light-grid', 'Intel Xeon', lightPrices, lightSpecs);
            generatePlanCards('budget-grid', 'AMD EPYC', budgetPrices, lightSpecs);
            generatePlanCards('performance-grid', 'Ryzen 9', performancePrices, lightSpecs);
            generateBotCards('bot-grid', botPlans, 'Bot');
            generateBotCards('voice-grid', botPlans, 'Sound');
            animate();
        }

        function animate() {
            ctx.clearRect(0, 0, canvas.width, canvas.height);
            particles.forEach(p => { p.update(); p.draw(); });
            requestAnimationFrame(animate);
        }

        init();
    </script>
</body>
</html>

